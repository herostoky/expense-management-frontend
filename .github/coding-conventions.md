Use TypeScript with strict typing.

Use PascalCase for component and type names.

Use kebab-case for file names.

Use camelCase for variables and props.

Use `@/` as an import alias for internal modules.

Group imports: external → internal → styles.

Prefer `varName` destructuring over dot access.

Use self-closing tags where appropriate.

Avoid deep relative imports like `../../../`.

Use TailwindCSS utility-first classes for styling.

Compose Tailwind classes: layout → spacing → color → effects.

Avoid inline styles.

Always use curly braces for blocks and put opening curly brace on the same line.

Use `className`, not `class`.

Use `onClick`, not `onclick`.

Use `aria-*` attributes for accessibility.

Use `alt` attributes for images.

Always hover cusor on buttons and links.

Use <Button> for buttons, not <a> or <div>.

Use <Link> for navigation, not <a> or <div>.

Use <Image> for images, not <img>.

Use <Input> for inputs, not <input>.

Use <Select> for selects, not <select>.

Use <Textarea> for textareas, not <textarea>.

Use <Modal> for modals, not <div> or <span>.

Use <Tooltip> for tooltips, not <div> or <span>.

Use <Popover> for popovers, not <div> or <span>.

Use <Dropdown> for dropdowns, not <div> or <span>.

Use <Accordion> for accordions, not <div> or <span>.

Use <Tabs> for tabs, not <div> or <span>.
